# Repo conventions — any agent runtime

This repo **is** the agent and skills library. The working conventions live in [CLAUDE.md](CLAUDE.md) and apply to every agent working here regardless of runtime (Claude Code, Codex, or anything else that reads this file) — read `CLAUDE.md` before making changes.

This file is deliberately a pointer: it previously carried a drifted copy of an old single-file source-of-truth doctrine, and duplicating `CLAUDE.md` here would just recreate that drift.
