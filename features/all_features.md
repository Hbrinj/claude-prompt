# All Features

| Feature | Branch | Summary | Status | Merged |
|---------|--------|---------|--------|--------|
| add-grill-plan-skill | feature/add-grill-plan-skill | Replace researcher + planner agents with single grill-plan skill that produces TDD-shaped plans for software work and ordered-step plans for non-software work. Collapses 3-step workflow to 2 steps. | Merged | 2026-05-03 |
| add-go-developer | feature/add-go-developer | Add agents/go-developer.md — a Go developer agent with strict testing, error handling, concurrency, and module hygiene rules. Ships with the reference Slice-aware execution section; integrated into SYSTEM_PROMPT.md routing. | Merged | 2026-05-03 |
| extend-slice-aware-to-other-agents | feature/extend-slice-aware-to-other-agents | Extend the canonical ## Slice-aware execution section verbatim from go-developer into android, ios, flutter, and kotlin-backend developer agents — slice/TDD discipline now enforced agent-side for every supported stack. | Merged | 2026-05-03 |
| split-kotlin-backend-aws-rules | feature/split-kotlin-backend-aws-rules | Split kotlin-backend's consolidated ## AWS rules section into 5 topic-specific top-level sections (AWS SDK, AWS service selection, IAM, Lambda, DynamoDB) following the go-developer precedent — sharp discoverability per Decision 10 of add-go-developer. | In Review | — |
| defer-out-of-scope-to-todo | feature/defer-out-of-scope-to-todo | Add `## Deferred (out of scope)` section to grill-plan task files; consolidate to repo-root /TODO.md at termination (additive, idempotent, source plan untouched). Surfaces Deferred: N in status postscript and resume summary. Existing TODO.md migrated to new schema. | Merged | 2026-05-03 |
| agent-frontmatter | feature/agent-frontmatter | Add YAML frontmatter (`name:` + `description:`) to all 9 agent definitions so Claude Code can auto-discover them as subagents. Bundles two new TODO entries (lock-down agent tools, PR-split-strategy) and the in-progress pr-split-strategy plan + checkpoint. | Merged | 2026-05-03 |
| parallel-sub-agents | feature/parallel-sub-agents | Add feature-level parallel-dispatch convention via new `skills/parallel-dispatch.md` — coordinator fans out ≥2 features in one message with `isolation: "worktree"` + `run_in_background: true`, concurrency cap of 3, per-feature gates in completion order, combined-diff reviewer pass after all PRs open. Developer agents now own the code-review loop end-to-end (new `## Self-review before return` section across all 5 stack agents); CLAUDE.md non-negotiable rule reworded; one new TODO row for pre-dispatch overlap detection. | Merged | 2026-05-04 |
| reviewer-agents | feature/reviewer-agents | Add two new reviewer agents alongside existing `code-reviewer`: `prompt-definition-reviewer` (covers `agents/`/`skills/` prompt files, structural & convention compliance) and `general-reviewer` (catch-all for docs, configs, plan/feature-log files). Wire both into SYSTEM_PROMPT.md Step 2 via a new "coordinator file-type routing gate" sub-step; non-negotiable rule generalised to "NEVER push without all relevant reviewers having APPROVED". 5 follow-on reviewer/abstraction items deferred to /TODO.md. | Merged | 2026-05-04 |
| parallel-docker-workers | feature/parallel-docker-workers | Add `skills/parallel-docker-dispatch.md` — Docker variant of `parallel-dispatch` where each worker runs inside a `claude-worker` container (Dockerfile + entrypoint + host wrapper) with full in-container freedom. Opt-in via "in containers" / "with docker" trigger phrasing; cap-of-3 inherited from worktree dispatch; 30-min hard timeout with `--rm` cleanup; structured `.worker-result.json` contract. Ships canary fixture + 6 smoke tests (image, entrypoint, single dispatch, parallel-with-cap, skill index, routing prose); 3 follow-ups deferred to /TODO.md. | Merged | 2026-05-05 |
| dispatch-docker-mac-root | fix/dispatch-docker-mac-root | Make `claude-worker` image run as non-root by default so `claude --dangerously-skip-permissions` no longer refuses on the macOS dispatch path (no `--user` flag). Reuse the existing `node` user (uid 1000) from the `node:lts-slim` base image; re-point `HOME`/`GOPATH` and `chown /workspace` accordingly. Linux dispatch unchanged — still overrides via `--user $(id -u):$(id -g)`. `scripts/test-image.sh` now asserts default uid is non-zero. | Merged | 2026-05-05 |
